module.exports = {
    autoprefixer: {
        options: {
            title: 'Build complete',  // optional
            message: '<%= package.name %> build finished successfully.' //required
        }
    }
};